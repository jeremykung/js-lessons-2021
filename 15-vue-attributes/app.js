const app = Vue.createApp({
    data() {
        return {
            // Variables for app
            url: "https://search.brave.com/",
            showDanger: true,
        }
    },
    methods: {
        // Functions for app
        
    }
})

app.mount('#app')